// import QuotationItem from "./QuotationItem";

// const QuotationCard = ({ card, margin }) => (
//   <div className="border p-4 my-4 rounded-lg shadow-sm">
//     {card.items?.length > 0 ? (
//       card.items.map((item, index) => (
//         <QuotationItem key={index} item={item} margin={margin} />
//       ))
//     ) : (
//       <p className="text-gray-500">No items in this card.</p>
//     )}
//   </div>
// );

// export default QuotationCard;
